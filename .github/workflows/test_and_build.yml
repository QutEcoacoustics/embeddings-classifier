name: Test and Build

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      
      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pyarrow pytest pytest-cov pytest-mock
      
  build:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install pytest for host integration tests
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-mock
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: parquet-processor:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Unit Test in Docker container
        run: |
          docker run --rm \
            -e BAW_AUTH_TOKEN="${{ secrets.ECOSOUNDS_API_TOKEN }}" \
            -v ${{ github.workspace }}:/app \
            --entrypoint pytest \
            parquet-processor:latest \
            tests/unit/ -v

      - name: Integration Test form host
        run: |
          pytest -m docker tests/docker